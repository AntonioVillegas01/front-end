import React from 'react';
import Layout from "../components/ui/layout";

const ContactPage = () => {
    return (
        <Layout>
            ContactPage
        </Layout>
    );
};

export default ContactPage;
